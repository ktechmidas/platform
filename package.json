{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna exec npm ci && npm run build && lerna bootstrap --hoist=grpc* --force-local --ci",
    "link": "lerna link --force-local",
    "build": "npm run link && lerna run build",
    "configure": "./scripts/configure_dashmate.sh",
    "start": "./packages/dashmate/bin/dashmate group:start --wait-for-readiness --verbose",
    "stop": "./packages/dashmate/bin/dashmate group:stop",
    "clean_data": "./scripts/clean_data.sh",
    "setup_local": "./scripts/setup_local.sh",
    "reset_data": "npm run clean_data && npm run configure && npm run setup_local",
    "test": "lerna run test --scope platform-test-suite",
    "build_and_test": "npm run build && npm run configure && npm run start && npm run test && npm run stop"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
